## precog_testing


### Setting it up

- Install `tensorflow` and `opencv 3` on your system before installing this
- Run `sudo python setup.py install` to install the package


### Using the package

- `import precog_testing` to import this package